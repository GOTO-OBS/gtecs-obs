# Database parameters
DATABASE_DIALECT = string(default=mysql)
DATABASE_USER = string(default=goto)
DATABASE_PASSWORD = string(default=None)
DATABASE_HOST = string(default=localhost)
DATABASE_ECHO = integer(default=0)
DATABASE_PRE_PING = integer(default=1)

# Scheduling parameters
DEFAULT_MIN_ALT = float(default=30)
DEFAULT_MAX_SUNALT = float(default=-12)
DEFAULT_MAX_MOON = string(default='B')
DEFAULT_MIN_MOONSEP = float(default=30)

WEIGHTING_WEIGHT = float(default=1)
AIRMASS_WEIGHT = float(default=1)
TTS_WEIGHT = float(default=0)

HARD_ALT_LIM = float(default=10)
HARD_HA_LIM = float(default=8)
READOUT_TIME = float(default=10)
TEMPLATE_REQUIREMENT = string(default='ANY')

# Scheduler parameters
PYRO_HOST = string(default='localhost')
PYRO_PORT = integer(default=9035)
PYRO_TIMEOUT = integer(default=5)

SCHEDULER_CHECK_PERIOD = float(default=5)
WRITE_QUEUE_FILE = integer(default=1)
WRITE_QUEUE_PAGE = integer(default=0)
SCHEDULER_SKIP_DAYTIME = integer(default=1)
SCHEDULER_SUNALT_LIMIT = float(default=-10.0)

# Slack bot parameters
ENABLE_SLACK = integer(default=0)
SLACK_BOT_TOKEN = string(default=slack_token)
SLACK_DEFAULT_CHANNEL = string(default=slack_channel)
